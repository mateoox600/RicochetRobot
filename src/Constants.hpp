#pragma once

#include <string>
#include "raylib-cpp.hpp"

namespace Constants {

    const int SCREEN_WIDTH = 800;
    const int SCREEN_HEIGHT = 800;
    const std::string TITLE_NAME = "Ricochet Robots";
    const int CELL_SIZE = 50;
    const raylib::Vector2 halfCell(25, 25);

}